import Typed from 'typed.js';

var options = {
  strings: ["I'm a Senior Full-Stack Engineer", 'I Build Web And Mobile Applications'],
  typeSpeed: 120,
  backSpeed: 50,
  backDelay: 1500,
  loop: true
};

var typed = new Typed('#headline', options);
